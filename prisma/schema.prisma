generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Agendamento {
  id       String @id @default(uuid())
  nome     String
  servico  String
  data     String
  hora     String
}